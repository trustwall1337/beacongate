// Top-level build file. Plugin versions live here so the :app
// module's build.gradle.kts can apply them by id without specifying
// a version each time.

plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
